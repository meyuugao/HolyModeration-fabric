## План: убрать обфускацию и вырезать whitelist/blacklist логику

### Часть A. Убрать обфускацию полностью

В `build.gradle` плагина обфускации уже нет, но в 15 файлах остались аннотации `@DontObf` / `ObfRule` и импорты пакета `me.yuugao.obfuscator`, которого нет в исходниках. Их нужно убрать, иначе проект не соберётся.

**A1. Удалить импорты и аннотации обфускации в файлах:**
1. `HolyModeration.java` — убрать `import me.yuugao.obfuscator.*` и `@DontObf(...)` над `onInitialize()`
2. `client/HolyModerationClient.java` — убрать импорты и `@DontObf(...)` над `onInitializeClient()`
3. `client/mixin/GameRendererMixin.java` — убрать импорты и `@DontObf(ObfRule.MAP_FIELD)` над полем `client`
4. `client/mixin/MinecraftClientMixin.java` — убрать импорты и аннотации
5. `client/mixin/MouseMixin.java` — убрать импорты и `@DontObf(...)` над `getX()`/`getY()`
6. `client/mixin/ScreenMixin.java` — убрать импорты и `@DontObf(ObfRule.MAP_FIELD)` над полем `client`
7. `client/gui/screen/AnimatedGuiScreen.java` — убрать импорты и `@DontObf(...)` над `init()`/`render()`/`close()`/`removed()`
8. `client/gui/screen/impl/MainGuiScreen.java` — убрать импорты и аннотации
9. `client/gui/drawable/render/PivotMode.java` — убрать импорты и аннотации
10. `client/gui/drawable/render/RenderMode.java` — убрать импорты и аннотации
11. `client/util/service/eventbus/EventBusService.java` — убрать импорты и аннотации (если есть)
12. `client/util/service/eventbus/Subscribe.java` — убрать импорты и `@DontObf(...)` над интерфей
13. `client/util/service/NotificationType.java` — убрать импорты и аннотации
14. `client/util/service/config/impl/KeyBindsConfig.java` — убрать импорты и `@DontObf(...)` над enum/полями
15. `client/modules/impl/SettingsModule.java` — убрать импорты и аннотации

### Часть B. Вырезать whitelist/blacklist логику (полное удаление)

**B1. Удалить классы целиком:**
- `client/util/service/ModValidationService.java`
- `client/util/service/UserValidationService.java`
- `client/util/viewer/FrameViewer.java` (использовался только для блокировки через `UserValidationService.stop()`)

**B2. `client/di/module/impl/ServicesModule.java`** — убрать 3 строки регистрации:
- `container.register(ModValidationService.class, ...)`
- `container.register(UserValidationService.class, ...)`
- `container.register(FrameViewer.class, ...)`
- Убрать ставшие ненужными импорты

**B3. `client/HolyModerationClient.java`** — убрать:
- импорт `UserValidationService`
- вызов `di.get(UserValidationService.class).onMinecraftStart();` в `onInitializeClient()`

**B4. `client/modules/impl/StateModule.java`** — убрать:
- поле `private final UserValidationService userValidationService;`
- импорт `UserValidationService`
- вызов `userValidationService.onJoinServer();` в `onServerConnect()`

**B5. `client/util/service/NetService.java`** — убрать:
- `getWhiteList()` 
- `getBlackList()`
- `sendLaunchData(String hwid, String username)`
- приватный `getListAsync(String url)` (используется только whitelist/blacklist)
- Убрать ставшие ненужными импорты (`TypeToken`, `Type` если больше не нужны)

### Часть C. Проверка

**C1.** Убедиться через `grep`, что нигде не осталось ссылок на `me.yuugao.obfuscator`, `ModValidationService`, `UserValidationService`, `FrameViewer`, `getWhiteList`, `getBlackList`, `sendLaunchData`, `onMinecraftStart`, `onJoinServer`.

**C2.** Попытка сборки `gradlew.bat build` (или `compileJava`) для подтверждения, что проект компилируется без обфускатора и без удалённой логики.

### Что НЕ трогаем
- `HwidService` и расчёт HWID — он используется модулем `TwinksCheckModule` (функционал проверки твинков), это бизнес-логика, не валидация.
- Остальную сетевую логику (journal API, checkouts, sounds, viewer jar download — хотя downloadViewerJar станет неиспользуемым после удаления FrameViewer, но оставлю метод как есть, либо уберу если он использовался только из FrameViewer).

### Результат
- Мод собирается без внешнего обфускатора, весь код открыт и читаем.
- Whitelist/blacklist валидация, блокировка мода, закрытие клиента и отправка HWID/ника на сторонний сервер при входе полностью удалены.